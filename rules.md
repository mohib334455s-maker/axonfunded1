<!--
Optional: replace this file with your full authoritative rulebook (≥ ~400 characters of real rules text).
While this stub is short, the Axon AI API automatically injects the live rules built from
`src/lib/axon-rules-content.ts` (same content as the /rules page).
-->

# rules.md (stub)

Paste your complete **Trading Rules** markdown here. The chat API reads **`rules.md` from the project root on every request** when the file contains enough content; otherwise it falls back to the built-in rule export.
