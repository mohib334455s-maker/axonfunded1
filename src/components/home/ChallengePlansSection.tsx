"use client";

import SupremePlanAPricing from "@/components/pricing/SupremePlanAPricing";

/** Home pricing — Plan A grid (same data as `/challenge`). */
export default function ChallengePlansSection() {
  return (
    <div className="py-24 px-4 sm:px-6 bg-background">
      <SupremePlanAPricing />
    </div>
  );
}
